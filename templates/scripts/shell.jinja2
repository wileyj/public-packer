#!/usr/bin/sh
export PATH="/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin"
rm -rf /usr/share/doc/*
rm -rf /usr/share/man/*
for i in `ls /var/log`; do
    if [ -f "/var/log/$i" ]; then
        echo /var/log/$i
    fi
done
