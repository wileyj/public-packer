#!/bin/sh
export PATH="/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin"
cat << EOF > /usr/sbin/runit_bootstrap
#!/bin/bash
export > /etc/envvars
exec /sbin/runsvdir-start &
EOF

chown root:root /usr/sbin/runit_bootstrap
chmod 755 /usr/sbin/runit_bootstrap
